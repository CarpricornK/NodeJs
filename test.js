const settimeouttest = () => {

    const b = Math.random() * 100;

        setTimeout(() => {
            console.log('settimeouttest');
        }, b);
    }

const settimeouttest2 = () => {

    const b = Math.random() * 100;

        setTimeout(() => {
            console.log('settimeouttest2');
        }, b);
    }    

const settimeouttest3 = () => {

    const b = Math.random() * 100;

        setTimeout(() => {
            console.log('settimeouttest');
        }, b);
    }

    settimeouttest();
    settimeouttest2();
    settimeouttest3();